import SettingsPlaceholder from "./SettingsPlaceholder";

export default function SettingsDanger() {
  return <SettingsPlaceholder title="Danger zone" description="Export data, change risky defaults, or delete the account." />;
}
