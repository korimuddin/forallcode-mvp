import SettingsPlaceholder from "./SettingsPlaceholder";

export default function SettingsWorkspace() {
  return <SettingsPlaceholder title="Workspace" description="Configure your desk, focus mode, and workspace defaults." />;
}
